_format_version: "2.1"

services:
- name: "APICompositionService"
  url: https://google.com
  plugins:
  - name: kong-api-composition
    config:
      aggregateResp: False
  routes:
  - name: route-42
    paths:
    - /
