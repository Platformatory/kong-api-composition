_format_version: "1.1"

services:
- name: API Composition Service
  url: https://google.com
  plugins:
  - name: kong-api-composition
    config:
      destinations:
        - uri:
          method:
          headers:
          body:
      options:
      aggregate: True  
  routes:
  - name: route-42
    paths:
    - /
